const path = require('path');

module.exports = {
  "config": path.resolve('./bot/config', 'config.json'),
  "models-path": path.resolve('./bot/models'),
  "seeders-path": path.resolve('./bot/seeders'),
  "migrations-path": path.resolve('./bot/migrations')
};